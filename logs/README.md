# logs

This directory is reserved for run logs and ad hoc diagnostics.

- log files here should be generated artifacts, not source inputs
- nothing in `logs/` should be required for the public site or core backend contract
